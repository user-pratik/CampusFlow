# Backend API URL — NOT required for local development.
# The frontend uses Next.js rewrites (next.config.ts) to proxy /api/* to the backend.
# Only set this if deploying frontend and backend on separate domains.
# NEXT_PUBLIC_API_URL=http://localhost:8000
